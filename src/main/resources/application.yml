server:
  port: 80
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/mydb
    username: root
  jpa:
    database-platform: ''
    hibernate:
      ddl-auto: create
    show-sql: true
